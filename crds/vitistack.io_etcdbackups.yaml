---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: etcdbackups.vitistack.io
spec:
  group: vitistack.io
  names:
    kind: EtcdBackup
    listKind: EtcdBackupList
    plural: etcdbackups
    shortNames:
    - eb
    singular: etcdbackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Target cluster name
      jsonPath: .spec.clusterName
      name: Cluster
      type: string
    - description: Storage type
      jsonPath: .spec.storageLocation.type
      name: Storage
      type: string
    - description: Current phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Last successful backup
      jsonPath: .status.lastBackupTime
      name: Last Backup
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: EtcdBackup is the Schema for the EtcdBackup API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EtcdBackupSpec defines the desired state of an etcd backup
            properties:
              clusterName:
                description: ClusterName is the name of the Kubernetes cluster to
                  backup
                type: string
              retention:
                default: 7
                description: Retention specifies how many backups to retain
                type: integer
              schedule:
                description: Schedule is the cron schedule for automated backups (e.g.,
                  "0 */6 * * *" for every 6 hours)
                type: string
              storageLocation:
                description: StorageLocation specifies where to store the backup
                properties:
                  bucket:
                    description: Bucket is the bucket name for cloud storage
                    type: string
                  path:
                    description: Path is the path/prefix within the storage location
                    type: string
                  secretRef:
                    description: SecretRef references a secret containing storage
                      credentials
                    type: string
                  type:
                    description: Type is the storage type (e.g., "s3", "gcs", "azure",
                      "local")
                    enum:
                    - s3
                    - gcs
                    - azure
                    - local
                    type: string
                required:
                - type
                type: object
            required:
            - clusterName
            - storageLocation
            type: object
          status:
            description: EtcdBackupStatus defines the observed state of an etcd backup
            properties:
              backupCount:
                description: BackupCount is the current number of stored backups
                type: integer
              backupSize:
                description: BackupSize is the size of the last backup in bytes
                type: string
              conditions:
                description: Conditions represent the latest available observations
                  of the backup's state
                items:
                  description: EtcdBackupCondition describes the state of an etcd
                    backup at a certain point
                  properties:
                    lastTransitionTime:
                      description: LastTransitionTime is the last time the condition
                        transitioned
                      format: date-time
                      type: string
                    message:
                      description: Message is a human-readable message indicating
                        details about the transition
                      type: string
                    reason:
                      description: Reason is a brief reason for the condition's last
                        transition
                      type: string
                    status:
                      description: Status is the status of the condition (True, False,
                        Unknown)
                      type: string
                    type:
                      description: Type is the type of the condition
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastBackupTime:
                description: LastBackupTime is the timestamp of the last successful
                  backup
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  status
                type: string
              nextBackupTime:
                description: NextBackupTime is the scheduled time for the next backup
                  (if scheduled)
                format: date-time
                type: string
              phase:
                description: Phase represents the current phase of the backup (Pending,
                  Running, Completed, Failed)
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
