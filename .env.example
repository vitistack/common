# Copy this file to .env or .env-<environment> and adjust values
# The ENV variable selects the overlay file .env-<ENV> if present
# Example:
#   ENV=dev
# will cause .env-dev to be loaded after .env

# Logging
LOG_JSON_ENABLED=false
LOG_COLORIZE_ENABLED=true
LOG_ADD_CALLER=true
LOG_LEVEL=debug
LOG_UNESCAPE_MULTILINE=true
LOG_DISABLE_STACKTRACE=false

# S3 Client Configuration
# The S3 client uses the MinIO SDK which is compatible with all S3-compatible providers.
#
# Use mock client for testing (default: true)
# Set to false to use real S3 client
S3_MOCK=true
#
# ============================================================
# PROVIDER-SPECIFIC EXAMPLES
# ============================================================
#
# --- AWS S3 ---
# S3_ENDPOINT=s3.amazonaws.com
# S3_REGION=us-east-1
# S3_USE_SSL=true
# S3_PATH_STYLE=false
#
# --- MinIO (self-hosted) ---
# S3_ENDPOINT=minio.example.com:9000
# S3_REGION=us-east-1
# S3_USE_SSL=false
# S3_PATH_STYLE=true
#
# --- Hetzner Object Storage ---
# S3_ENDPOINT=fsn1.your-objectstorage.com  # or nbg1, hel1
# S3_REGION=fsn1
# S3_USE_SSL=true
# S3_PATH_STYLE=true
#
# --- DigitalOcean Spaces ---
# S3_ENDPOINT=nyc3.digitaloceanspaces.com  # or sfo3, ams3, sgp1, fra1
# S3_REGION=nyc3
# S3_USE_SSL=true
# S3_PATH_STYLE=false
#
# --- Wasabi ---
# S3_ENDPOINT=s3.wasabisys.com  # or s3.eu-central-1.wasabisys.com
# S3_REGION=us-east-1
# S3_USE_SSL=true
# S3_PATH_STYLE=true
#
# --- Backblaze B2 ---
# S3_ENDPOINT=s3.us-west-004.backblazeb2.com
# S3_REGION=us-west-004
# S3_USE_SSL=true
# S3_PATH_STYLE=true
#
# --- Cloudflare R2 ---
# S3_ENDPOINT=<account_id>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_USE_SSL=true
# S3_PATH_STYLE=true
#
# ============================================================

# Endpoint URL (required) - see examples above
S3_ENDPOINT=
# AWS region (default: us-east-1)
S3_REGION=us-east-1
# Access key for authentication (required)
S3_ACCESS_KEY_ID=
# Secret key for authentication (required)
S3_SECRET_ACCESS_KEY=
# Optional session token for temporary credentials
S3_SESSION_TOKEN=
# Use HTTPS (default: true)
S3_USE_SSL=true
# Use path-style addressing - required for MinIO, Hetzner, etc. (default: false)
S3_PATH_STYLE=false
# Connection timeout (default: 10s)
S3_CONNECT_TIMEOUT=10s
# Request timeout (default: 30s)
S3_REQUEST_TIMEOUT=30s
# Maximum retry attempts (default: 3)
S3_MAX_RETRIES=3
# Enable debug logging (default: false)
S3_DEBUG=false
# Default bucket name
S3_BUCKET=